#ifndef TOKENTYPE_H_INCLUDED
#define TOKENTYPE_H_INCLUDED

enum Type
{
IMPORTN,
RETURN,
ARGLIST,
FUNCTIONEND,
FUNCTION,
FUNCTIONHEAD,
FUNCTIONBODY,
CMD,
VARDEC,
ROOT,
FUNCNAME,
CMDSEQ,
TOKENEND
};

enum TokenType
{
    TEXT,
    NUMBER,
    ELSEKEYWORD,
    ENDKEYWORD,
    VAR,
    OPAREN,
    CPAREN,
    COMMA,
    COLON,
    EQUALS,
    TERM,
    IFKEYWORD,
    QUOTE
};
#endif // TOKENTYPE_H_INCLUDED
